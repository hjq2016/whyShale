server:
  port: 1991
  servlet:
      context-path: /askService
spring:
  application:
    name: ask-service
  cloud:
    nacos:
      config:
        file-extension: yml
        server-addr: 127.0.0.1:8848
        shared-configs[0]:
          data-id: whyshare-config-share.yml
          refresh: true
      discovery:
        server-addr: 127.0.0.1:8848